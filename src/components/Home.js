import Fancy from './children';
import ThemeToggle from './themetoggle';
export default function Home() {
	const a = 1 
	const b = 2
	const c = 3
	const d = false                       // 6 toán tử trả ra false là : 0, null, undefined, '', false
	const result = a > 0 && !!!d || c < 0  // toán tử and và or đằng sau sẽ quyết định là có lấy giá trị trước nó hay kiểm tra tiếp
	console.log(result)
return < div>
<Fancy>
	<h1>dialog-title</h1>
	<ThemeToggle/>
</Fancy>
</div>
}

